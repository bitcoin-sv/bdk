# Rust build output / intermediates (not committed)
/module/rustbdk/target/
/test/rust/target/
/test/rust/Cargo.lock
/build-wasm/
/build-wasm-deps/

# Verification corpus generated by the toolchain-free node runner in test/types
# (`npm run test:parity`). Never committed: it is regenerated from the module.
/test/types/.corpus.json

# NOTE: the in-source merged archive (module/rustbdk/bdk-sys/lib/libbdkffi_<os_arch>.a)
# is intentionally NOT ignored. It is a generated artifact that is not checked in
# during the experimental stage, but it stays visible in `git status` (untracked)
# and is expected to be committed in-source later, like module/gobdk's libGoBDK_*.a.
